# BUGS — Symptom · Repro · Root Cause · Fix · Test

One place bugs live. Verified findings come from `DEVPLAN.md` §Teil 2; add new rows in
the same format as they're found. `Status` is `open` / `fixed` / `needs-design`.

| ID | Symptom | Repro (Schritte/Handle) | Root Cause + Ort | Status | Fix-Commit | Test |
|----|---------|--------------------------|-------------------|--------|------------|------|
| B1 | Gold→Prisma-Trade zeigt fälschlich auch Gold/Silber statt nur Prisma | Trade auf `tiers:['prisma']` erzwingen (`PickScene.ts:246`/`:280`) bei vollem `prismaSlots`-Cap | Fallback in `rollOneOffer` (`augments/offers.ts:50`) ignoriert `opts.tiers` komplett, wenn der Pool leer ist; Pool wird oft leer weil `prismaAllowed` (`offers.ts:36`) auch für erzwungene Trades gilt | fixed | S1-1 | `test/augments/offers.test.ts` |
| B2 | Transmute-Augment verschwindet nicht nach dem Auslösen; `MAX_AUGMENTS` kann überschritten werden | `__CC.grant('transmutprisma'); __CC.goto(2)` mehrfach, Slot-Anzahl beobachten | `Transmute: Prisma/Chaos/Silver` (`augments/gold.ts:773`, `augments/prisma.ts:924`/`939`) granten neue Augments, riefen aber nie `removeAugment(self.id)`; `run.addAugment` (`core/run.ts:60`) prüfte `MAX_AUGMENTS` (=6) nicht. Zusätzlich fiel bei der self-removal auf: `AugmentManager.init()` iterierte das live `run.augments`-Array — ein `removeAugment` mitten in `onCombatInit` hätte den nachrückenden Nachbar-Eintrag diese Runde übersprungen | fixed | S1-2 | `test/augments/transmute.test.ts` |
| B3 | Aim-Preview zeigt für alle Q-Fähigkeiten denselben Pfeil, auch bei Kreis-/Selbst-AoEs | Zac auswählen, Q anvisieren — Pfeil statt Ring | `ArenaScene.ts:~1034` zeichnet hartkodiert Strip+Linie+Pfeilspitze für jede Q, unabhängig vom tatsächlichen Formtyp; keine Shape-Daten am Kit (siehe `champions/types.ts`) | fixed | S2-3 | `test/champions/kits.test.ts` (26/26 Champions haben `spec.q`, gültiger `kind`); manueller Headless-Stichprobencheck über 8 Champions quer über self/circle(self)/circle(cursor)/line/cone/dash (Zac, Lux, Ashe, Teemo, Ziggs, Nocturne, Fiddlesticks, Taric), 0 Konsolenfehler |
| B4 | Ausgewählte Augment-Karten kommen nach Reroll nicht mehr / Karte scheint kaputt | Repro (Vitest, `test/augments/offers.test.ts` "reroll dead-end repro + invariants (B4)"): `rollOneOffer(1, exclude)` mit `exclude` = alle Silber-Augment-Ids (round 1 → `allowedTiers` = `['silber']`) → liefert `null`. Vorher blieb die Karte in `PickScene.rerollSlot`/`rerollTradeGold` dabei unverändert UND der Reroll-Button klickbar — der Spieler sah keine Reaktion und konnte beliebig oft klicken, ohne je ein Ergebnis zu bekommen. Tier-Band-Wechsel als Ursache wurde geprüft und **ausgeschlossen**: `offerRound` (`PickScene.ts`) ist beim initialen Roll (`ArenaScene.ts`/`EndScene.ts`: `rollOffers(run.round)` vor `run.round++`) und beim Reroll (`offerRound = run.round - 1`) identisch | `PickScene.rerollSlot`/`rerollTradeGold` gaben bei `null`-Rückgabe still auf (`if (!def) return;`) statt den Button sichtbar zu deaktivieren | fixed | S1-3 | `test/augments/offers.test.ts` ("reroll dead-end repro + invariants (B4)": Repro, `nextOfferAfterReroll` Erfolg/Exhausted-Fälle, Tier-Invariante, Owned-Invariante, Exclude-Invariante) |
| B5 | „Das mit den Sternen funktioniert nicht" | Goldene Spatel im Inventar anschauen — ★-Icon, aber kein Effekt | Es existiert **kein** Sterne-/Upgrade-System im Code, nur das ★-Icon der Goldenen Spatel (`items/registry.ts:871`). Kein Bug, sondern fehlendes/nie gebautes Feature — braucht eine Design-Entscheidung (bauen vs. Icon entfernen), siehe Ticket S6-1 | needs-design | | |
| B6 | Champion-Beschreibungstexte weichen von der tatsächlichen Kit-Wirkung ab | Stichprobe: Zahlen in `champions/kits.ts` `info.*.desc` gegen die Zahlen im jeweiligen `fireQ`/`castE`-Body vergleichen | `info.*.desc` waren bereits reiner Flavor-Text ohne eingebettete Zahlen (kein 1:1-Drift-Fund), aber es gab keine sichtbare Zahlen-/Form-Wahrheit überhaupt — die Q-Zahlen lebten nur unsichtbar in `rs(a,b,c,d)`-Literalen im Effekt-Code. `describe.ts` generiert jetzt Text aus `spec.q` (Form) + `Q_SCALE`/`Q_SCALE_LABEL` (Zahlen, `kits.ts`); `fireQ`/`onAutoHit` lesen dieselben `Q_SCALE`-Tupel per `rs(...Q_SCALE.<id>)` statt eigener Literale — eine Quelle, kein Duplikat möglich | fixed | S2-4 | `test/champions/describe.test.ts` (26/26 Champions haben nicht-leeren generierten Text; Stichprobe über 6 Champions/alle Shape-Kinds — line/circle-self/cone/self/circle-cursor±radius — exakter String-Vergleich; Mutations-Test beweist: Ändern von `Q_SCALE.zac` ändert den generierten Text ohne `describe.ts` anzufassen) |
| B7 | Viele Augments „funktionieren nicht" (spürbar) | `npm run matrix` (`scripts/effect-matrix.mjs`, S3-1): grantet jedes Augment isoliert (frischer Run) und prüft die Wirkung, die seine eigenen Capability-Daten (`ruleFlags`/`statMods`/`hooks`) implizieren — `ruleFlag`-Klasse exakt (Flag-Wert nach Grant), `statMod`-Klasse exakt gegen die Stat-Pipeline `(base+flat)*(1+pct)`, `hooks`-Klasse als DPS-Delta gegen einen Mehrfach-Lauf-Baseline-Median (Spieler wird headless über `player.x/y`+`castQ/E/dash` an den nächsten Gegner herangeführt, da kein Input-Event-Loop läuft). **Aktueller Stand (100% Abdeckung, 144/144, 0 Skips, 0 Konsolenfehler): 100 PASS / 44 FAIL.** Vollständige Zeile-für-Zeile-Liste in `effect-matrix.json` (Repo-Root, committed als Artefakt für S3-2). Ein Fund ist bereits exakt (nicht nur das erwartungsgemäß verrauschte `hooks`-Rauschen): `gigantwuchs` (prisma, statMod) erwartet `damage=110.00`, misst `135.00` — deterministischer Mismatch, kein Baseline-Rauschen. Die 44 `hooks`-FAILs sind laut Harness-Doku *Kandidaten für manuelle Prüfung* (DPS-Delta ist bei nicht-Schaden-Augments (Schilde/CC/Utility) grundsätzlich aussagelos), keine bewiesenen Bugs | „Feuert ohne Crash" ≠ „hat den beschriebenen, spürbaren Effekt in richtiger Höhe" — es fehlte eine Wirkungs-Assertion pro Augment (`augments/*`); S3-1 baut nur die Matrix/misst, fixt nichts | open (Matrix seit S3-1 vorhanden; Fixes/Streichungen der 44 FAILs sind S3-2) | S3-1 | `scripts/effect-matrix.mjs` (`npm run matrix`); Ergebnis-Artefakt `effect-matrix.json` |
| B8 | `run.addItem` konnte `gold` unter 0 drücken, wenn es direkt (ohne `ShopScene`s eigenen Gold-Check davor) aufgerufen wird | `newRun(); run.gold = 50; addItem({cost: 999, ...})` — vor dem Fix: `gold` wurde negativ, Item trotzdem gebucht | `core/run.ts:111` (`addItem`) prüfte die Erschwinglichkeit nicht selbst, sondern verließ sich einzig auf den Aufrufer (`ShopScene.tryBuy`, `ShopScene.ts:198`: `run.gold < it.cost`) — ein zweiter/direkter Aufrufer (Debug-Handle, künftiger Code) hätte das Gold-Invariant brechen können | fixed | S3-3 | `test/core/economy.test.ts` ("addItem() refuses a purchase the run cannot afford…", "gold never goes negative…") |
