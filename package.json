{
  "name": "lyareth",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "verify": "npm run build && npm run test && node scripts/verify.mjs",
    "matrix": "npm run build && node scripts/effect-matrix.mjs",
    "sim": "npm run build && node scripts/sim.mjs",
    "shots": "node scripts/shots.mjs",
    "report": "node scripts/report.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "playwright": "1.49",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.10"
  }
}
